# Node / Capacitor tooling
node_modules/
package-lock.json

# Generated staging + native project (regenerated by CI: build:web + cap add).
# Anchored to the repo root: a bare `android/` also matches
# fastlane/metadata/android/, which silently swallowed the store listing.
www/
/android/
/ios/

# Build output
*.apk
